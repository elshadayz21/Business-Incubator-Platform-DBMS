SESSION_SECRET=
NODE_ENV=
PORT=
DB_USER=
DB_HOST=
DB_PASS=
DB_NAME=
DB_PORT=5432

# Optional. Hours of inactivity before an in-progress application draft gets
# a one-time reminder email. Defaults to 24 if unset.
APPLICATION_REMINDER_DELAY_HOURS=24

